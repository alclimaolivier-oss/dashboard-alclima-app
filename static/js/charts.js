import {esc,number} from './utils.js';
export function bars(items,{unit='',empty='Aucune donnée sur cette période.'}={}){if(!items.length)return `<p class="muted chart-empty">${esc(empty)}</p>`;const maximum=Math.max(...items.map(i=>Number(i.value)||0),1);return `<div class="bar-chart" role="list">${items.map((i,n)=>`<div class="bar-row" role="listitem"><div class="bar-label"><span>${esc(i.label)}</span><strong>${esc(number(i.value))}${unit?' '+esc(unit):''}</strong></div><div class="bar-track"><span class="bar-fill color-${n%4}" style="width:${Math.max(0,Math.min(100,Number(i.value)/maximum*100))}%"></span></div></div>`).join('')}</div>`;}
export function progress(value,target){const pct=target>0?Math.max(0,Math.min(100,value/target*100)):0;return `<div class="progress" role="progressbar" aria-label="Progression de l’objectif" aria-valuenow="${Math.round(pct)}" aria-valuemin="0" aria-valuemax="100"><span style="width:${pct}%"></span></div>`;}
